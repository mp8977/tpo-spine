<%= form_for(@diagnose_has_medicine) do |f| %>
  <% if @diagnose_has_medicine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@diagnose_has_medicine.errors.count, "error") %> prohibited this diagnose_has_medicine from being saved:</h2>

      <ul>
      <% @diagnose_has_medicine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
