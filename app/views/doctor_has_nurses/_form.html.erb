<%= form_for(@doctor_has_nurse) do |f| %>
  <% if @doctor_has_nurse.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@doctor_has_nurse.errors.count, "error") %> prohibited this doctor_has_nurse from being saved:</h2>

      <ul>
      <% @doctor_has_nurse.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
