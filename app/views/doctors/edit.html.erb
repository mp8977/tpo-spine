<div class="text-center">
  <h1>Osebni profil</h1>

  <% if  !(doctor_signed_in? or admin_signed_in?) %>
      <%= link_to("Preskoci", { controller: "doctors/registrations", action: "new" }) %>
  <% end %>

  <%= render 'form' %>

  <!-- <%= link_to 'Show', @doctor %>
  <%= link_to 'Back', doctors_path %> -->
</div>