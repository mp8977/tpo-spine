<%= form_for(@measurement_category) do |f| %>
  <% if @measurement_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@measurement_category.errors.count, "error") %> prohibited this measurement_category from being saved:</h2>

      <ul>
      <% @measurement_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :elements %><br>
    <%= f.number_field :elements %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
