<%= form_for(@measurement_home) do |f| %>
  <% if @measurement_home.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@measurement_home.errors.count, "error") %> prohibited this measurement_home from being saved:</h2>

      <ul>
      <% @measurement_home.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
