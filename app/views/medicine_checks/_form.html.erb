<%= form_for(@medicine_check) do |f| %>
  <% if @medicine_check.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@medicine_check.errors.count, "error") %> prohibited this medicine_check from being saved:</h2>

      <ul>
      <% @medicine_check.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
