<%= form_for(@medicine_instruction) do |f| %>
  <% if @medicine_instruction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@medicine_instruction.errors.count, "error") %> prohibited this medicine_instruction from being saved:</h2>

      <ul>
      <% @medicine_instruction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table align="center">
    <td align="right">URL:</td>
    <td align="left"><%= f.text_field :url_string, required: true %></td>
    </tr>
    <tr>
      <td align="right">Izbrisana</td>
      <td align="left"><%= f.collection_select(:deleted, %w(true false), :to_s, :to_s, {include_blank: false},{:multiple => false, required: true}) %></td>
    </tr>
  </table>

  <div class="actions">
    <%= f.submit class: "btn btn-info", value: "Poslji" %>
  </div>
<% end %>
