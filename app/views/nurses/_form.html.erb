<%= form_for(@nurse) do |f| %>
  <% if @nurse.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nurse.errors.count, "error") %> prohibited this nurse from being saved:</h2>

      <ul>
      <% @nurse.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <table align="center">
      <tr>
        <td align="right">*Email:</td>
        <td>
          <%= f.text_field :email, :required => true, :type => 'email', :pattern => '([^@\s]+)@((?:[-a-z0-9]+\.)+[a-z]{2,})' %>
        </td>
      </tr>
      <tr>
        <td align="right">*Geslo:</td>
        <td>
          <%= f.text_field :password, :required => true, :type => 'password', :pattern => '(?=.*[a-zA-Z])(?=.*[0-9]).{6,40}' %>
        </td>
      </tr>
      <tr>
        <td align="right">Sifra medicinske sestre:</td>
        <td><%= f.text_field :nurseNumber %></td>
      </tr>
      <tr>
        <td align="right">Priimek:</td>
        <td><%= f.text_field :lastName %></td>
      </tr>
      <tr>
        <td align="right">Ime:</td>
        <td><%= f.text_field :firstName %></td>
      </tr>
      <tr>
        <td align="right">Telefon:</td>
        <td><%= f.text_field :phone %></td>
      </tr>
    </table>

    <div class="actions">
      <%= f.submit class: "btn btn-success", value: "Poslji" %>
    </div>
<% end %>
