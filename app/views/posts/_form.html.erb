<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table align="center">
    <tr>
      <td align="right">Naziv poste</td>
      <td align="left"><%= f.text_field :postName, required: true %></td>
    </tr>
    <tr>
      <td align="right">Postna stevilka</td>
      <td align="left"><%= f.text_field :postNumber, required: true %></td>
    </tr>
  </table>

  <div class="actions">
    <%= f.submit class: "btn btn-info", value: "Poslji" %>
  </div>
<% end %>
